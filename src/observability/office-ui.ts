export function officeHtml(nonce:string){return `<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Agent Office · 업무 현황</title>
<style>:root{color-scheme:dark;--bg:#0b1018;--panel:#131d2b;--line:#2a394d;--text:#eaf2fc;--muted:#9cafc4;--accent:#a5ec81;--warn:#ffc56f}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:14px/1.5 system-ui,sans-serif}header{display:flex;justify-content:space-between;align-items:center;gap:20px;padding:16px 24px;border-bottom:1px solid var(--line)}h1{font-size:19px;margin:0}a{color:var(--accent)}button,textarea{font:inherit;color:inherit}button{background:#1b2a3d;border:1px solid #465973;border-radius:9px;padding:9px 13px;cursor:pointer}button:hover{border-color:var(--accent)}button:disabled{opacity:.45;cursor:not-allowed}.primary{background:var(--accent);color:#132310;border-color:var(--accent)}.danger{color:var(--warn)}main{display:grid;grid-template-columns:minmax(300px,350px) minmax(0,1fr);min-height:calc(100vh - 65px)}aside{border-right:1px solid var(--line);padding:20px;overflow:auto}section.detail{padding:24px;max-width:1100px;width:100%;margin:auto}.eyebrow{font-size:11px;letter-spacing:.13em;text-transform:uppercase;color:var(--muted);margin-bottom:12px}.health{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:20px}.health span,.pill{font-size:11px;background:#1e2e42;border-radius:6px;padding:4px 7px}.work{width:100%;display:block;text-align:left;margin:8px 0;padding:13px;border-radius:12px}.work.selected{border-color:var(--accent);background:#1d3140}.work strong{display:block;font-size:14px;margin:3px 0 7px}.work small,.muted{color:var(--muted)}.work .rail{margin-top:9px}.rail{height:8px;background:#29384a;border-radius:9px;overflow:hidden}.rail>i{display:block;height:100%;background:var(--accent);transition:width .25s}.head{display:flex;justify-content:space-between;gap:12px;align-items:start}.head h2{margin:0 0 9px;font-size:25px;overflow-wrap:anywhere}.head small{color:var(--muted)}.actions{display:flex;gap:8px;flex-wrap:wrap}.notice{margin:14px 0;padding:12px;background:#1a2a3c;border-left:3px solid var(--warn);border-radius:6px}.metrics{display:flex;gap:18px;margin:22px 0;color:var(--muted)}.metrics b{color:var(--text);font-size:19px}.steps{display:grid;gap:9px;margin:20px 0}.step,.desk-card{border:1px solid var(--line);background:var(--panel);border-radius:12px;padding:14px}.step .top{display:flex;gap:10px;justify-content:space-between;align-items:center}.step b{font-size:15px}.step p{margin:8px 0;color:var(--muted);overflow-wrap:anywhere}.step small{color:var(--muted)}.step.done{border-color:#467d5d}.step.running{border-color:#3e7297}.step.attention{border-color:#8b6a43}.step button{margin-top:10px}.desk-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin:22px 0}.desk-card h3{margin:0 0 10px}.desk-card p{overflow-wrap:anywhere;margin:8px 0}.desk-card ul{padding-left:19px;margin:8px 0}.desk-card li{margin:5px 0;overflow-wrap:anywhere}.desk-card details{margin-top:9px}.run-list{display:flex;gap:7px;flex-wrap:wrap}.run-list button.active{border-color:var(--accent);color:var(--accent)}.evidence-list{max-height:300px;overflow:auto}.editor{border:1px solid var(--accent);background:#15271f;border-radius:12px;padding:15px;margin:12px 0}.editor textarea{width:100%;min-height:92px;background:#0d1720;border:1px solid var(--line);padding:10px;border-radius:8px;resize:vertical}.editor .actions{margin-top:8px}.timeline{border-top:1px solid var(--line);margin-top:28px;padding-top:15px}.timeline article{border-left:2px solid var(--line);padding:0 0 15px 14px}.timeline time{color:var(--muted);font-size:11px}.empty{color:var(--muted);padding:22px;border:1px dashed var(--line);border-radius:10px}.status{color:var(--accent)}@media(max-width:760px){main{display:block}aside{border-right:0;border-bottom:1px solid var(--line);max-height:330px}section.detail{padding:17px}.head{display:block}.actions{margin:12px 0}header{padding:14px}.desk-grid{grid-template-columns:1fr}}</style></head><body><header><h1>Agent Office <small class="muted">내 업무 책상</small></h1><nav><a href="settings">연결 및 설정</a> · <a id="connections" href="connections">사이트 로그인</a></nav></header><main><aside><div class="eyebrow">맡긴 업무</div><div id="health" class="health"></div><div id="works"></div><p class="muted">연결된 에이전트가 시작한 업무만 보입니다. 새 업무는 연결된 에이전트에게 요청하세요.</p></aside><section class="detail"><div id="detail"></div><div id="message" role="status" aria-live="polite"></div></section></main>
<script nonce="${nonce}">
let state=null,selected=new URL(location.href).searchParams.get('work'),selectedRun=new URL(location.href).searchParams.get('run'),editing=null,draft='';const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const labels={pending:'대기',leased:'진행 중',succeeded:'검증 완료',failed:'실패',needs_human:'검토 필요',skipped_deadline:'시간 초과',running:'진행 중',completed:'완료',partial_evidence:'부분 완료',turn_completed:'대화 턴 완료 · 업무 미확인',approved:'승인됨 · 업무 미확인',waiting_auth:'로그인 대기',retryable_failure:'재시도 대기'};
const healthNames={'MCP':'컴퓨터','Browser / VM':'브라우저','CLI':'개발 도구','Decision Plane':'AI 판단'},healthStates={active:'작동 중',configured:'설정됨',not_configured:'미설정',ready:'준비됨',unavailable:'사용 불가'};
function shortTitle(value){const full=String(value??'').replace(/\s+/g,' ').trim();const end=full.search(/[.!?](?:\s|$)/);const first=end>=12?full.slice(0,end):full;return first.length>64?first.slice(0,64).trimEnd()+'…':first}
function work(){const group=state?.works.find(x=>x.id===selected)||state?.works[0]||null;if(!group)return null;const detail=(state.run_details||[]).find(x=>x.id===group.id&&x.run_id===selectedRun)||group;return {...detail,runs:group.runs||[]}}function choose(id){selected=id;selectedRun=null;editing=null;draft='';history.replaceState(null,'','?work='+encodeURIComponent(id));render()}function chooseRun(id){selectedRun=id;editing=null;draft='';history.replaceState(null,'','?work='+encodeURIComponent(selected)+'&run='+encodeURIComponent(id));render()}
function render(){if(!state)return;if($('instruction'))draft=$('instruction').value;const w=work();selected=w?.id??null;const loginCount=(state.website_connections||[]).filter(x=>x.state!=='ready'&&x.state!=='retry_requested'||x.handoff).length;$('connections').textContent=loginCount?'사이트 로그인 '+loginCount+'개 필요':'사이트 로그인';$('health').innerHTML=state.health.map(x=>'<span title="'+esc(x.detail)+'">'+esc(healthNames[x.label]||x.label)+' · '+esc(healthStates[x.state]||x.state)+'</span>').join('');$('works').innerHTML=state.works.map(x=>'<button class="work '+(x.id===selected?'selected':'')+'" data-work="'+esc(x.id)+'"><small>'+esc(x.kind.toUpperCase())+' · '+esc(x.paused?'일시정지':labels[x.status]||x.status)+'</small><strong>'+esc(shortTitle(x.title))+'</strong><small>'+(x.progress_percent===null?'단계별 진행률 미관측':x.verified_steps+' / '+x.total_steps+' 단계 검증 완료')+'</small><div class="rail"><i style="width:'+(x.progress_percent??0)+'%"></i></div></button>').join('')||'<p class="empty">아직 맡긴 업무가 없습니다.</p>';document.querySelectorAll('[data-work]').forEach(b=>b.onclick=()=>choose(b.dataset.work));
if(!w){$('detail').innerHTML='<p class="empty">업무가 시작되면 여기에서 단계별 진척도와 멈춘 이유를 확인할 수 있습니다.</p>';return}
let out='<div class="head"><div><div class="eyebrow">업무 상세 · '+esc(w.kind)+'</div><h2>'+esc(shortTitle(w.title))+'</h2><small>'+esc(w.paused?'사용자가 일시정지함':labels[w.status]||w.status)+' · 마지막 변경 '+esc(new Date(w.updated_at||Date.now()).toLocaleString())+'</small></div><div class="actions">'+(w.can_pause?'<button id="pause" class="'+(w.paused?'primary':'danger')+'">'+(w.paused?'이어가기':'일시정지')+'</button>':'')+'</div></div>';
if(shortTitle(w.title)!==w.title)out+='<details class="notice"><summary>전체 요청 보기</summary><p>'+esc(w.title)+'</p></details>';
if(w.attention_reason)out+='<div class="notice">멈춘 이유: '+esc(w.attention_reason)+'</div>';out+='<div class="metrics"><span><b>'+(w.progress_percent===null?'—':w.progress_percent+'%')+'</b><br>검증된 진척도</span><span><b>'+(w.progress_percent===null?'—':w.verified_steps+' / '+w.total_steps)+'</b><br>완료 단계</span></div><div class="rail"><i style="width:'+(w.progress_percent??0)+'%"></i></div><p class="muted">'+esc(w.progress_basis)+'</p>';
const repeat=w.runs||[];out+='<div class="desk-grid"><section class="desk-card"><h3>업무 지시서</h3>'+(w.brief?'<p>'+esc(w.brief.request)+'</p><p class="muted">완료 기준: '+esc(w.brief.completion_rule)+'</p><p class="muted">현재 목표: '+esc(w.brief.current_goal)+'</p>':'<p class="muted">이 실행 경로는 구조화된 업무 지시서를 저장하지 않았습니다.</p>')+'</section><section class="desk-card"><h3>실행 계획</h3>'+(w.plan?'<p>'+esc(w.plan.summary)+'</p><p class="muted">계획 작성: '+esc(w.plan.planner_model)+' · 병렬 상한 '+esc(w.plan.max_concurrency)+'명 · 단계 '+esc(w.total_steps)+'개</p><p class="muted">계획에 기록된 배정과 의존관계만 표시합니다. 모델의 내부 추론은 저장하지 않습니다.</p>':'<p class="muted">저장된 실행 계획 없음</p>')+'</section><section class="desk-card"><h3>실행 이력</h3><div class="run-list">'+repeat.map((r,i)=>'<button data-run="'+esc(r.run_id)+'" class="'+(r.run_id===w.run_id?'active':'')+'" title="'+esc(r.run_id)+'">'+(i===0?'최근 실행':'이전 실행 '+i)+' · '+esc(labels[r.status]||r.status)+'</button>').join('')+'</div><p class="muted">동일한 계획 ID로 실행한 기록만 한 업무에 묶습니다. 현재 목록에 '+repeat.length+'건 표시.</p></section><section class="desk-card"><h3>결과와 반복 방법</h3>'+(w.outcome?'<p>'+(w.outcome.verified?'<span class="status">모든 단계 검증 완료</span>':'아직 최종 결과 미검증')+'</p><p>'+esc(w.outcome.summary||'결과 요약이 저장되지 않았습니다.')+'</p>'+(w.outcome.last_review?'<p class="muted">최근 검토: '+esc(w.outcome.last_review)+'</p>':''):'<p class="muted">검증 결과 미관측</p>')+(w.method?'<details><summary>반복 방법 · '+esc(w.method.status==='candidate'?'초안':'검증됨')+'</summary><p class="muted">'+esc(w.method.basis)+'</p><p class="muted">저장된 계획의 역할·의존관계를 재사용할 수 있지만, 자동 Task Pack 승격을 뜻하지 않습니다.</p></details>':'')+'</section></div>';
out+='<h3>진행 단계</h3><div class="steps">'+w.stages.map(s=>'<article class="step '+(s.verified?'done':s.status==='leased'?'running':s.status==='pending'?'':'attention')+'"><div class="top"><b>'+esc(s.label)+' · '+esc(s.id)+'</b><span class="pill">'+esc(labels[s.status]||s.status)+'</span></div><p>'+esc(s.objective)+'</p>'+(w.progress_percent===null?'<small class="muted">이 경로의 단계별 진척도 미관측</small>':'<div class="rail" role="progressbar" aria-label="'+esc(s.label)+' 단계 검증" aria-valuemin="0" aria-valuemax="100" aria-valuenow="'+(s.verified?100:0)+'"><i style="width:'+(s.verified?100:0)+'%"></i></div>')+'<small>'+esc(s.owner?'현재 실행: '+s.owner:'담당 실행기: '+s.executor)+' · 시도 '+(w.progress_percent===null?'미관측':s.attempts+'회')+(s.instruction_version?' · 수정 지침 v'+s.instruction_version:'')+'</small>'+(s.can_edit?'<br><button data-edit="'+esc(s.id)+'">이 단계 지침 수정</button>':'')+'</article>').join('')+'</div>';
if(w.plan)out+='<section class="desk-card evidence-list"><h3>계획 배정 · 자료와 근거</h3>'+w.stages.map(s=>'<details><summary>'+esc(s.id)+' · '+esc(s.role||s.executor)+' · '+esc(labels[s.status]||s.status)+'</summary><p class="muted">담당: '+esc(s.executor)+(s.depends_on?.length?' · 선행 단계: '+esc(s.depends_on.join(', ')):' · 바로 시작')+'</p><p>완료 확인: '+esc((s.completion_evidence||[]).join(' / ')||'미기록')+'</p>'+(s.source_urls?.length?'<p class="muted">계획된 출처: '+esc(s.source_urls.join(' · '))+'</p>':'')+(s.result?'<p>실행 보고: '+esc(s.result.summary)+'</p><p class="muted">독립 확인: '+esc(s.result.readback?.verified?'확인됨 · '+s.result.readback.method:'미확인')+' · 품질: '+esc(s.quality?.accepted?'승인':'미승인')+'</p><ul>'+s.result.evidence.map(e=>'<li>'+esc(e.claim)+' · '+esc(e.verification)+' · '+esc(e.source_url)+'</li>').join('')+s.result.artifacts.map(a=>'<li>산출물 '+esc(a.kind)+' · '+esc(a.summary)+' · SHA-256 '+esc(a.sha256.slice(0,12))+'…</li>').join('')+'</ul>':'<p class="muted">아직 실행 결과가 없습니다.</p>')+'</details>').join('')+'</section>';
if(editing){const s=w.stages.find(x=>x.id===editing);if(s?.can_edit)out+='<div class="editor"><h3>'+esc(s.label)+' 단계에 새 지침 전달</h3><p class="muted">아직 시작하지 않은 읽기 전용 단계만 바꿀 수 있습니다. 이미 실행 중이면 먼저 일시정지하세요.</p><p class="notice">영향 범위: '+esc(s.downstream?.length?s.downstream.join(', ')+' 후속 단계가 새 결과를 받습니다. 각 후속 단계의 기존 목표 문구는 자동으로 바뀌지 않으므로 확인하세요.':'이 단계만 변경됩니다.')+' 이전에 검증한 결과는 유지됩니다.</p><textarea id="instruction" maxlength="2000" placeholder="예: 카드뉴스 대신 핵심 요약문으로 제공해줘"></textarea><div class="actions"><button id="cancel-edit">취소</button><button id="save-edit" class="primary">다음 실행에 적용</button></div></div>'}
out+='<section class="timeline"><h3>인계·진행 기록</h3>'+(w.events.length?w.events.slice(-30).reverse().map(e=>'<article><time>'+esc(new Date(e.created_at).toLocaleString())+'</time><div>'+esc(e.worker_id||'전체 업무')+' · '+esc(e.kind)+'</div><small class="muted">'+esc(e.detail)+'</small></article>').join(''):'<p class="muted">기록 없음</p>')+'</section>';$('detail').innerHTML=out;if($('instruction'))$('instruction').value=draft;
$('pause')?.addEventListener('click',()=>action(w.paused?'resume':'pause',w));document.querySelectorAll('[data-run]').forEach(b=>b.onclick=()=>chooseRun(b.dataset.run));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{editing=b.dataset.edit;draft='';render();$('instruction')?.focus()});$('cancel-edit')?.addEventListener('click',()=>{editing=null;draft='';render()});$('save-edit')?.addEventListener('click',()=>{const instruction=$('instruction').value.trim();if(!instruction){$('message').textContent='새 지침을 입력해 주세요.';return}action('edit',w,editing,instruction)});
}
async function action(kind,w,worker_id,instruction){$('message').textContent='적용 중…';try{const response=await fetch('office/action',{method:'POST',headers:{'content-type':'application/json','x-agent-driver':'human-office'},body:JSON.stringify({run_id:w.run_id,action:kind,revision:w.revision,worker_id,instruction})});const data=await response.json();if(!response.ok)throw Error(data.error||'변경 실패');editing=null;await refresh();$('message').textContent=kind==='edit'?'다음 배정에 새 지침이 적용됩니다.':kind==='pause'?'새 배정을 멈췄습니다.':'다시 진행합니다.'}catch(error){$('message').textContent=String(error.message||error)}}
async function refresh(){const response=await fetch('office/snapshot',{cache:'no-store'});if(!response.ok)throw Error('업무 현황 연결 실패');state=await response.json();render()}refresh().catch(error=>$('message').textContent=String(error));const stream=new EventSource('office/events');stream.addEventListener('snapshot',event=>{state=JSON.parse(event.data);if(editing){const step=work()?.stages.find(x=>x.id===editing);if(step?.can_edit)return;editing=null;draft='';$('message').textContent='단계 상태가 바뀌어 수정이 중단되었습니다.'}render()});stream.onerror=()=>$('message').textContent='실시간 연결 재시도 중';
</script></body></html>`;}
